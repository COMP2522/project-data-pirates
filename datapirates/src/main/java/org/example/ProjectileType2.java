package org.example;

import processing.core.PVector;

import java.awt.*;

public class ProjectileType2 extends Projectile
{
    public ProjectileType2(PVector pos, PVector direction, float size, float speed, Color clr, Window scene) {
        super(pos, direction, size, speed, clr, scene);
    }
}
